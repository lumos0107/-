# 00. 프로젝트 개요
> 길동무 — 제주도 러닝 경로 추천 서비스

---

## 서비스 한 줄 정의

사용자의 현재 위치, 목표 거리, 시간대, 난이도를 입력받아
Beam Search k=3 기반으로 루프형 러닝 경로 3개를 추천하고,
실시간 페이스 측정 및 러닝 기록 저장까지 제공하는 웹/앱 서비스.

---

## 기술 스택

| 파트 | 기술 |
|------|------|
| 백엔드 | Spring Boot, Spring Data JPA, MySQL Connector, Lombok, Gradle |
| 데이터베이스 | AWS RDS MySQL 8.0 (공간 인덱스) |
| 프론트엔드 | Expo (React Native Web) — 앱 + 웹 동시 지원 |
| 인프라 | AWS EC2 (백엔드), AWS RDS (DB), AWS S3 (프론트) |
| 외부 API | Google Routes API, Google Maps JS API, Google Places API, Google Elevation API, OSM Overpass API |

---

## AWS 인프라 정보

| 항목 | 값 |
|------|-----|
| RDS 엔드포인트 | `gildongmu.cvi84aoey86n.ap-northeast-2.rds.amazonaws.com` |
| RDS DB명 | `gildongmu` |
| RDS 사용자 | `admin` |
| EC2 퍼블릭 IP | `43.200.178.203` |
| EC2 OS | Ubuntu 22.04 LTS |
| Java | OpenJDK 17 |

---

## 전체 데이터 흐름

```
[배포 전 1회 수집 — 완료]
OSM 도로망 → jeju_road_points (15m 재표집)
Google Elevation → elevation_meters 저장
OSM 장애물 → obstacles
Google Places → places

[사용자 요청 시]
GPS 위치 수신
→ 공간 인덱스로 jeju_road_points 반경 50m 조회
→ obstacles, places 조회
→ Beam Search k=3 + Score 함수로 경로 3개 생성
→ Google Routes API 1회 호출 (실제 도로 경로 확정)
→ 프론트 지도에 표시
→ 러닝 시작 → GPS 실시간 추적 → 기록 저장
```

---

## 핵심 API 목록

| 메서드 | 경로 | 역할 |
|--------|------|------|
| POST | `/api/routes/recommend` | 경로 추천 |
| POST | `/api/runs/start` | 러닝 시작 |
| POST | `/api/runs/update` | 실시간 위치 업데이트 |
| POST | `/api/runs/end` | 러닝 종료 저장 |
| GET | `/api/runs/history` | 기록 조회 |

---

## 파일 구성

```
00_프로젝트_개요.md        ← 현재 파일
01_프로젝트_세팅.md        ← Spring Boot 초기 설정
02_데이터베이스_테이블.md  ← 11개 테이블 DDL + 설명
03_DB_엔티티.md            ← JPA Entity 클래스
04_Haversine_유틸.md       ← 거리 계산 유틸
05_Score_함수.md           ← 경로 평가 Score 함수
06_BeamSearch_알고리즘.md  ← Beam Search k=3 경로 생성
07_REST_API.md             ← Controller / Service / Repository
08_GoogleRoutes_연동.md    ← Google Routes API 연동
09_EC2_배포.md             ← 빌드 및 EC2 배포
```
