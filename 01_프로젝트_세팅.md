# 01. Spring Boot 프로젝트 세팅

---

## 프로젝트 생성

VS Code에서 아래 순서로 생성한다.

```
1. Ctrl+Shift+P → "Spring Initializr: Create a Gradle Project"
2. Spring Boot 버전: 3.x
3. 언어: Java
4. Group: com.gildongmu
5. Artifact: gildongmu
6. Packaging: Jar
7. Java 버전: 17
```

### 의존성 선택

```
Spring Web
Spring Data JPA
MySQL Driver
Lombok
```

---

## 폴더 구조

```
src/main/java/com/gildongmu/
├── GildongmuApplication.java       ← 진입점
├── controller/
│   ├── RouteController.java        ← 경로 추천 API
│   └── RunController.java          ← 러닝 기록 API
├── service/
│   ├── RouteService.java           ← Beam Search 경로 생성
│   └── RunService.java             ← 러닝 기록 처리
├── algorithm/
│   ├── BeamSearchAlgorithm.java    ← Beam Search k=3
│   ├── ScoreCalculator.java        ← Score 함수
│   └── HaversineUtil.java          ← 거리 계산
├── entity/
│   ├── JejuRoadPoint.java
│   ├── User.java
│   ├── Course.java
│   ├── CoursePoint.java
│   ├── CourseSegment.java
│   ├── Obstacle.java
│   ├── Place.java
│   ├── RunningRecord.java
│   └── RunningTrack.java
├── repository/
│   ├── JejuRoadPointRepository.java
│   ├── CourseRepository.java
│   ├── ObstacleRepository.java
│   ├── PlaceRepository.java
│   └── RunningRecordRepository.java
└── dto/
    ├── RouteRecommendRequest.java   ← 경로 추천 요청 DTO
    ├── RouteRecommendResponse.java  ← 경로 추천 응답 DTO
    ├── RunStartRequest.java
    └── RunEndRequest.java

src/main/resources/
└── application.properties
```

---

## application.properties

```properties
# DB 연결 (RDS)
spring.datasource.url=jdbc:mysql://gildongmu.cvi84aoey86n.ap-northeast-2.rds.amazonaws.com:3306/gildongmu?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
spring.datasource.username=admin
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA 설정
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# 서버 포트
server.port=8080

# Google API 키
google.routes.api.key=${GOOGLE_API_KEY}
```

> `${DB_PASSWORD}`, `${GOOGLE_API_KEY}` 는 환경변수로 주입.
> application.properties에 직접 쓰지 말 것 (깃허브 유출 방지).

---

## build.gradle 의존성

```groovy
dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'com.mysql:mysql-connector-j'
    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}
```

---

## CORS 설정

프론트(Expo 웹)에서 백엔드를 호출할 수 있도록 CORS를 허용한다.

```java
// config/CorsConfig.java
@Configuration
public class CorsConfig implements WebMvcConfigurer {

    // WebMvcConfigurer: Spring MVC 설정을 커스터마이징하는 인터페이스
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/api/**")
                .allowedOrigins("*")        // 모든 출처 허용 (배포 시 도메인 지정)
                .allowedMethods("GET", "POST", "PUT", "DELETE")
                .allowedHeaders("*");
    }
}
```

---

## 실행 확인

```bash
# 로컬 실행
./gradlew bootRun

# 브라우저에서 확인
http://localhost:8080
```
