# 09. EC2 배포
> 로컬에서 개발 완료 → jar 빌드 → EC2에 업로드 → 실행.
> EC2는 코드 짜는 곳이 아님. 빌드는 반드시 로컬에서.

---

## EC2 정보

| 항목 | 값 |
|------|-----|
| 퍼블릭 IP | `43.200.178.203` |
| OS | Ubuntu 22.04 LTS |
| Java | OpenJDK 17 |
| 포트 | 8080 |
| 키 파일 | `capstone-key.pem` |

---

## 1단계. 로컬에서 jar 빌드

```bash
# 프로젝트 루트에서 실행
./gradlew clean build

# 빌드 결과물 위치
build/libs/gildongmu-0.0.1-SNAPSHOT.jar
```

---

## 2단계. EC2에 jar 업로드

```bash
# scp: SSH를 통해 파일 전송하는 명령어
# -i: 키 파일 지정
scp -i capstone-key.pem \
    build/libs/gildongmu-0.0.1-SNAPSHOT.jar \
    ubuntu@43.200.178.203:~/gildongmu.jar
```

---

## 3단계. EC2 접속

**방법 1 — SSH (터미널)**
```bash
ssh -i capstone-key.pem ubuntu@43.200.178.203
```

**방법 2 — 브라우저 (EC2 Instance Connect)**
```
AWS 콘솔 → EC2 → capstone-ec2 → 연결 → EC2 Instance Connect → 연결
```

---

## 4단계. 환경변수 설정 후 실행

```bash
# 환경변수 설정 (DB 비밀번호, API 키)
# application.properties의 ${DB_PASSWORD}, ${GOOGLE_API_KEY}가 이 값으로 채워짐
export DB_PASSWORD="여기에비밀번호"
export GOOGLE_API_KEY="여기에API키"

# 백그라운드 실행
# nohup: 터미널 닫아도 계속 실행
# > app.log: 로그를 파일로 저장
# 2>&1: 에러 로그도 같이 저장
# &: 백그라운드 실행
nohup java -jar gildongmu.jar \
    --spring.profiles.active=prod \
    > app.log 2>&1 &

# 실행 확인
ps aux | grep gildongmu
```

---

## 5단계. 동작 확인

```bash
# EC2에서 직접 확인
curl http://localhost:8080/api/routes/recommend \
  -X POST \
  -H "Content-Type: application/json" \
  -d '{"latitude":33.4996,"longitude":126.5312,"targetDistance":3000,"timeOfDay":"day","difficulty":"beginner"}'

# 외부에서 확인 (로컬 또는 프론트에서)
curl http://43.200.178.203:8080/api/routes/recommend \
  -X POST \
  -H "Content-Type: application/json" \
  -d '{"latitude":33.4996,"longitude":126.5312,"targetDistance":3000,"timeOfDay":"day","difficulty":"beginner"}'
```

---

## 로그 확인

```bash
# 실시간 로그 보기
tail -f app.log

# 최근 100줄
tail -n 100 app.log

# 에러만 필터
grep "ERROR" app.log
```

---

## 재배포 절차

코드 수정 후 다시 배포할 때.

```bash
# 1. 로컬: 빌드
./gradlew clean build

# 2. 로컬: EC2에 업로드
scp -i capstone-key.pem \
    build/libs/gildongmu-0.0.1-SNAPSHOT.jar \
    ubuntu@43.200.178.203:~/gildongmu.jar

# 3. EC2: 기존 프로세스 종료
ssh -i capstone-key.pem ubuntu@43.200.178.203

# EC2 내부에서:
# 실행 중인 프로세스 PID 확인
ps aux | grep gildongmu

# PID로 종료 (PID를 실제 숫자로 교체)
kill <PID>

# 다시 실행
export DB_PASSWORD="여기에비밀번호"
export GOOGLE_API_KEY="여기에API키"
nohup java -jar gildongmu.jar > app.log 2>&1 &
```

---

## 보안 그룹 확인

EC2에서 8080 포트가 열려 있어야 외부에서 접근 가능.

```
AWS 콘솔 → EC2 → capstone-ec2 → 보안 탭 → 인바운드 규칙

필수 포트:
  22   (SSH)
  80   (HTTP)
  8080 (Spring Boot)
```

---

## 주의사항

- `capstone-key.pem` 파일 잃어버리면 EC2 접속 불가 → 안전한 곳에 보관
- `DB_PASSWORD`, `GOOGLE_API_KEY`는 환경변수로만 관리 → 깃허브에 절대 올리지 말 것
- 프리티어 유지: t2.micro, 월 750시간 이하
- 프로젝트 종료 후 RDS + EC2 반드시 삭제 (과금 방지)
