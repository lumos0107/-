# 10. 동일 경로 다양성 문제

> 같은 가중치로 빔서치 k=3을 실행하면 3개 루트가 동일한 경로로 수렴하는 문제

---

## 문제 원인

빔 3개가 동일한 출발점에서 동일한 가중치로 시작하면,  
매 이터레이션에서 전체 후보 중 점수 상위 3개를 뽑는데  
→ 같은 후보를 3번 복사하는 꼴이 되어 결국 동일 경로 생성.

```
이터 1: 후보점 A(점수 0.9), B(0.8), C(0.7)
         빔#0 → A, 빔#1 → A, 빔#2 → A  (전부 같음)
이터 2: 빔#0, #1, #2 모두 같은 위치에서 같은 후보 탐색
         → 영원히 동일한 경로
```

---

## 해결 아이디어

### 아이디어 1. 초기 방향 강제 분리 (가장 간단)

서버 시작 시 앵커를 8방향으로 나눠서 각 빔에 다른 앵커 배정.

```
빔#0 → 앵커: 북쪽 방향
빔#1 → 앵커: 동쪽 방향  
빔#2 → 앵커: 남쪽 방향
```

- 구현: `generateAnchorPoint()` 를 3개 만들어서 빔마다 다른 앵커 사용
- 장점: 구현 가장 쉬움
- 단점: 앵커 방향에 도로가 없으면 의미 없음

---

### 아이디어 2. 빔 간 다양성 페널티 (Diverse Beam Search)

후보 점수를 매길 때, 다른 빔이 이미 지나간 경로와 겹치면 페널티 추가.

```
score -= 다른 빔의 방문 포인트와 겹치는 정도 × 가중치
```

- 구현: 각 빔이 `otherBeams.visitedPoints` 를 참조해서 reuse 페널티 적용
- 장점: 자연스럽게 다양한 경로 탐색
- 단점: 빔끼리 참조가 생겨 코드 복잡도 증가

---

### 아이디어 3. 첫 스텝 강제 분기

이터레이션 1에서만 각 빔에 서로 다른 후보를 강제 배정.

```
이터 1: 
  후보를 점수순 정렬 후
  빔#0 → 1등 후보
  빔#1 → 이미 선택된 후보와 거리 > 100m 인 것 중 1등
  빔#2 → 빔#0, #1과 거리 > 100m 인 것 중 1등
이터 2~: 일반 빔서치 진행
```

- 구현: while 루프 시작 전 첫 이터레이션 별도 처리
- 장점: 초기 분기만 처리하면 이후 자연스럽게 다른 경로 탐색
- 단점: 초기 후보 중 거리 조건 맞는 게 없으면 실패

---

### 아이디어 4. 가중치에 소량의 랜덤 노이즈 추가

같은 가중치지만 빔마다 ±5% 랜덤 노이즈를 줘서 미세한 차이 발생.

```
빔#0: slope_weight = 1.5 × (1 + random(-0.05, 0.05))
빔#1: slope_weight = 1.5 × (1 + random(-0.05, 0.05))
빔#2: slope_weight = 1.5 × (1 + random(-0.05, 0.05))
```

- 구현: ScoreCalculator에 noise 파라미터 추가
- 장점: 구현 간단, 자연스러운 다양성
- 단점: 재현 불가 (매 요청마다 다른 결과), 노이즈가 작으면 여전히 비슷

---

### 아이디어 5. 가중치 프리셋 3종 (현재 계획된 방식)

빔마다 다른 성격의 가중치 프리셋 적용.

```
빔#0 — FLAT     : slope 가중치 ↑↑  → 평탄 경로
빔#1 — BALANCED : 현재 가중치 그대로 → 종합 최적
빔#2 — SCENIC   : facility 가중치 ↑↑ → 시설/경관 경로
```

- 구현: `WeightPreset` enum + `ScoreCalculator.calculate(preset, ...)` 
- 장점: 3개 루트가 명확히 다른 성격, 프론트에서 라벨로 표시 가능
- 단점: 가중치 튜닝 필요

---

## 권장 순서

```
단기 (지금 바로): 아이디어 1 (초기 방향 강제 분리)
                  → 구현 10분, 즉시 다양한 경로 확인 가능

중기 (테스트 후): 아이디어 5 (가중치 프리셋)
                  → 프론트 UI와 연결, 사용자 경험 개선

장기 (선택):      아이디어 2 (Diverse Beam Search)
                  → 같은 가중치에서도 다양성 보장, 완성도 높음
```

---

## 현재 상태

- 기존 BASIC 빔서치 정상 동작 ✅
- 기존 BASIC은 실제 RDS 제주 데이터에서 3개 루트가 순서까지 완전히 동일함 ⚠️
- 1번 개선안으로 두 방식을 실험함
  - `algorithm/direction`: 50m micro 방향분리
  - `algorithm/macro`: 430m macro 거점 기반 방향분리

---

## 2026-05-21 실험 결과

테스트 입력:
```text
startLat = 33.499
startLng = 126.531
targetDistance = 3000m
```

### 1. BASIC
```text
routeCount = 3
distance = 2265m 동일
sameSequence = true
middleOverlap = 1.0000 / 1.0000 / 1.0000
```

결론: 같은 점 리스트가 3번 복제됨.

### 2. micro DirectionSplit
```text
routeCount = 3
distance = 2859~2880m
sameSequence = false
middleOverlap = 0.8070 / 1.0000 / 0.8070
```

결론: 순서 복제는 깨졌지만 0~1500m 구간이 거의 동일함.

### 3. MacroDirection
```text
routeCount = 3
distance = 2929~2957m
sameSequence = false
middleOverlap = 0.4167 / 0.6800 / 0.7083
```

구간별 예시:
```text
route 0 vs 1
0-500m     overlap=1.0000
500-1000m  overlap=0.8571
1000-1500m overlap=0.0000
1500-2000m overlap=0.1667
2000-2500m overlap=0.5000
2500-2949m overlap=0.0000
```

결론: 출발부는 겹치지만 중간 구간에서 의미 있게 갈라짐. 현재 1번 개선안은 macro 방식이 가장 유효함.

---

## MacroDirection 작동 순서

```text
1. 출발점 근처 가장 가까운 도로 포인트 선택
2. 목표 거리 절반 부근에 앵커 포인트 생성
3. 내부 빔 12개 생성
4. 각 빔은 서로 다른 방향 오프셋을 가짐
5. 현재 위치에서 300~560m 떨어진 도로 포인트를 다음 거점 후보로 조회
6. 후보 거점을 점수화
   - 앵커 방향성
   - 430m 거점 거리 적합도
   - 재방문 패널티
   - 귀환 모드에서는 출발점 방향성
7. 선택된 거점까지 local path로 사이 도로 포인트 리스트 생성
8. 거점 도착 후 다음 거점 선택 반복
9. 앵커 통과 또는 목표거리 절반 진행 후 귀환 모드 전환
10. 출발점 근처로 돌아오면 루프 완성
11. 최종 3개 선택: 품질 45% + 다양성 35% + 거리 적합도 20%
```

### 현재 macro 주요 상수
```java
MACRO_STEP_METERS = 430.0
MACRO_MIN_METERS = 300.0
MACRO_MAX_METERS = 560.0
INTERNAL_BEAM_WIDTH = 12
FINAL_ROUTE_COUNT = 3
EARLY_WAYPOINT_MIN_DISTANCE = 250.0
NORMAL_WAYPOINT_MIN_DISTANCE = 180.0
```
