# 11. 알고리즘 앞으로 할 일

> 알고리즘 담당 작업 범위 정리
> 마지막 업데이트: 2026-05-21

---

## 현재 기준 상태

### 완료

| 항목 | 상태 | 비고 |
|------|------|------|
| 기존 BASIC 빔서치 | ✅ | 50m 단위 micro 탐색 |
| 알고리즘 모듈화 | ✅ | `basic`, `common`, `direction`, `macro` |
| micro DirectionSplit | ✅ | 3개 생성, 중간부 다양성은 부족 |
| MacroDirection | ✅ | 430m 거점 기반 방향분리 |
| 실제 RDS 비교 테스트 | ✅ | `RealJejuRouteComparisonTest` |

### 현재 가장 좋은 결과

테스트 입력:
```text
startLat = 33.499
startLng = 126.531
targetDistance = 3000m
```

MacroDirection 결과:
```text
routeCount = 3
distance = 2929~2957m
middleOverlap = 0.4167 / 0.6800 / 0.7083
```

---

## 1순위. MacroDirection 추가 튜닝

목표:
```text
3개 경로 유지
거리 3000m에 더 근접
출발 0~1000m 구간 다양성 개선
중간 overlap 0.6 이하로 안정화
```

작업 후보:

| 작업 | 목적 | 우선순위 |
|------|------|----------|
| 초반 0~500m 분리 강화 | 시작부터 같은 길로 몰리는 현상 완화 | 높음 |
| 방향 오프셋 재조정 | 좌/우 경로 균형 개선 | 중간 |
| local path 연결 품질 개선 | 거점 사이 점 리스트 자연스럽게 만들기 | 높음 |
| 최종 선택에서 middleOverlap 제한 강화 | 세 번째 경로 중복 방지 | 중간 |
| 실행 시간 최적화 | 현재 RDS 테스트 약 1분 30초 | 중간 |

현재 macro 주요 상수:
```java
MACRO_STEP_METERS = 430.0
MACRO_MIN_METERS = 300.0
MACRO_MAX_METERS = 560.0
INTERNAL_BEAM_WIDTH = 12
FINAL_ROUTE_COUNT = 3
EARLY_WAYPOINT_MIN_DISTANCE = 250.0
NORMAL_WAYPOINT_MIN_DISTANCE = 180.0
```

---

## 2순위. 가중치 프리셋 3종 구현

목표:
```text
같은 출발점에서도 서로 다른 성격의 경로 3개 생성
```

예상 프리셋:

| 프리셋 | 성격 | 가중치 방향 |
|--------|------|-------------|
| FLAT | 평탄한 경로 | slope penalty 강화 |
| BALANCED | 기본 균형 | 현재 기본값 |
| COMFORT | 편의시설/공원 선호 | facility bonus 강화 |

예상 작업:
```text
1. ScoreWeights 클래스 추가
2. ScoreCalculator.calculate(..., weights) 형태로 확장
3. algorithm/preset 패키지 생성
4. PresetWeightBeamSearchAlgorithm 구현
5. RDS 비교 테스트에 PRESET 추가
```

주의:
```text
가중치만 바꾸면 후보 도로망이 좁은 곳에서는 여전히 같은 경로가 나올 수 있음.
필요하면 MacroDirection과 결합 가능.
```

---

## 3순위. Diverse Beam Search 구현

목표:
```text
후보 선택 단계에서 이미 선택된 경로와 다른 후보를 우선 선택
```

핵심 아이디어:
```text
finalCandidateScore = baseScore - lambda * similarityToSelected
```

예상 작업:
```text
1. algorithm/diverse 패키지 생성
2. RouteOverlapUtil 또는 SimilarityCalculator 추가
3. 후보 선택 시 selected candidate와 거리/중복률 비교
4. 최종 경로 선택도 품질 + 다양성 기준으로 처리
5. BASIC / DirectionSplit / Macro / Diverse 비교
```

---

## 4순위. 알고리즘 비교 리포트 정리

목표:
```text
발표/보고서에 넣을 수 있는 실험 표 만들기
```

비교 항목:

| 항목 | 설명 |
|------|------|
| routeCount | 생성된 유효 경로 수 |
| distance | 목표 거리와의 차이 |
| sameSequence | 완전히 같은 점 리스트인지 |
| wholeOverlap | 전체 경로 중복률 |
| middleOverlap | 중간 60% 구간 중복률 |
| segmentOverlap | 500m 구간별 중복률 |
| runtime | 실행 시간 |

현재 비교 기준 테스트:
```text
src/test/java/com/gildongmu/algorithm/experiment/RealJejuRouteComparisonTest.java
```

---

## 5순위. RouteService 연결은 나중에

현재는 알고리즘 실험 단계이므로 프론트/API 선택 연결은 보류.

나중에 연결할 때 할 일:
```text
1. 요청 DTO에 algorithmType 추가
2. BASIC / MACRO / PRESET / DIVERSE 선택 가능하게 분기
3. 기본값은 안정적인 알고리즘으로 설정
4. 프론트에서 선택 없이 호출해도 기본 추천 3개 반환
```

---

## 당장 다음 작업 추천

```text
1. MacroDirection 초반 0~1000m 다양성 개선
2. macro local path 연결 로직 개선
3. 가중치 프리셋 3종 설계 시작
```

