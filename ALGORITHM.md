# 길동무 경로 추천 알고리즘 설명

## 개요

사용자가 출발 좌표와 목표 거리를 입력하면, 빔서치(Beam Search) 알고리즘이 제주도 도로 포인트 DB를 탐색해 **순환 경로(출발점으로 돌아오는 루프)** 를 3개 추천한다.

---

## 전체 흐름

```
사용자 요청
 (위도, 경도, 목표거리)
        ↓
   앵커포인트 계산
   (목표거리/2 거리의 방향별 후보 중 장애물 적은 지점)
        ↓
   빔 초기화 (3개 빔, 각 다른 방향으로 출발)
        ↓
   반복 탐색 (최대 800회)
   ┌─────────────────────────────────────┐
   │  현재 위치 반경 200m 내 후보 조회    │
   │  막다른 길 후보 제거 (exitCount < 2) │
   │  각 후보 점수 계산                   │
   │  빔별 최고점 후보 선택               │
   │  앵커포인트 통과 여부 체크           │
   │  목표거리 도달 시 완료 처리          │
   └─────────────────────────────────────┘
        ↓
   유효 루프 필터링
   (출발점 복귀 포함 총 거리 ≈ 목표거리 ±10%)
        ↓
   최종 루트 3개 반환
```

---

## 핵심 개념

### 빔서치 (Beam Search)
- 완전 탐색 대신 매 단계 **상위 K개 경로(빔)만 유지**하는 탐욕적 탐색
- K = 3 (beamWidth), 각 빔이 독립적으로 서로 다른 경로를 탐색
- 메모리와 속도의 균형을 맞춘 알고리즘

### 앵커포인트
- 루트가 출발점에서만 맴돌지 않게 유도하는 **중간 목표 지점**
- 목표거리의 절반(반환점) 거리에 있는 지점
- 8방향(0°, 45°, 90°, 135°, 180°, 225°, 270°, 315°) 중 장애물이 가장 적은 방향 선택
- 앵커를 통과하면 `targetPassed = true` → 이후 출발점으로 귀환 유도

### 경로 다양성
- 빔 #0 → 북쪽(0°), 빔 #1 → 남동쪽(120°), 빔 #2 → 남서쪽(240°) 으로 초기 출발
- 다른 빔이 지나간 포인트 근처(30m 이내)면 **다양성 패널티** 부여 → 3개 루트가 서로 다른 경로

---

## 점수 계산 (ScoreCalculator)

각 후보 포인트는 아래 8개 항목으로 점수를 계산해 가장 높은 점수의 후보를 선택한다.

| 항목 | 가중치 | 방향 | 설명 |
|------|--------|------|------|
| distance_fit | 2.0 | + | 현재 누적거리가 목표거리에 가까울수록 보너스 |
| slope_penalty | 1.5 | - | 경사도 5% 초과 시 패널티 |
| obstacle_penalty | 1.0 | - | 신호등(×50), 횡단보도(×20) 근처 패널티 |
| reuse_penalty | 1.2 | - | 이미 지나간 길(15m 이내) 재방문 패널티 |
| return_cost | 1.8 | - | 진행률 40% 이후 출발점에서 멀어질수록 패널티 |
| facility_bonus | 0.5 | + | 편의점/공원 등 시설 근처 보너스 |
| anchor_bonus | 1.0 | + | 앵커포인트에 가까울수록 보너스 (통과 전만) |
| direction_penalty | 0.3→0.1 | - | 급격한 방향 전환(U턴) 패널티 |

**최종 점수 = Σ(보너스 항목) - Σ(패널티 항목)**

### 진행률에 따른 return_cost 변화
```
0% ~ 40%  : return_cost 가중치 = 0   (자유롭게 탐색)
40% ~ 70% : return_cost 가중치 = 0.5 (귀환 슬슬 유도)
70% ~ 100%: return_cost 가중치 = 1.0 (강하게 귀환 유도)
```

---

## 주요 설정값 (BeamSearchConfig)

| 설정 | 값 | 의미 |
|------|----|------|
| beamWidth | 3 | 동시에 유지하는 경로 수 |
| maxIterations | 800 | 최대 탐색 반복 횟수 |
| toleranceRatio | 10% | 목표거리 허용 오차 (5km → ±500m) |
| stepRadius | 200m | 한 스텝에 탐색하는 반경 |
| minStepDistance | 1m | 최소 이동 거리 (제자리 방지) |
| obstacleQueryRadius | 100m | 장애물 조회 반경 |
| placeQueryRadius | 500m | 장소 조회 반경 |
| targetPassRadius | 30m | 앵커포인트 통과 판정 반경 |

---

## 막다른 길 가지치기

후보 포인트 선택 시, 해당 포인트에서 **다음 스텝으로 나갈 수 있는 출구가 2개 미만**이면 제외한다.

```java
// 이미 조회된 후보 목록 안에서만 확인 (전체 DB 재조회 없이 빠름)
long exitCount = candidates.stream()
    .filter(p -> d > minStep && d <= stepRadius && p ≠ current)
    .count();
if (exitCount < 2) continue;  // 막다른 길 제거
```

이 조건 덕분에 Mapbox 폴리라인에서 삐죽 튀어나오는 구간이 줄어든다.

---

## 유효 루트 판정

탐색 완료 후 아래 조건을 모두 만족하는 빔만 최종 결과로 반환한다.

```
|( 탐색거리 + 출발점까지 직선거리 ) - 목표거리| ≤ 허용오차
```

- 루프가 완성되지 않은 경로(중간에 멈춘 경로)는 제외
- 이 거리에 Mapbox 도로 스냅 거리는 포함되지 않음 (API 응답 시 보정)

---

## 파일 구조

```
algorithm/
├── BeamState.java              # 단일 빔(경로)의 상태 (루트, 거리, 완료여부)
├── CandidateScore.java         # 후보 포인트 + 점수 묶음
├── GeoDataCache.java           # DB 조회 결과 메모리 캐시
├── HaversineUtil.java          # 거리/경사 계산 유틸
├── ScoreCalculator.java        # 8가지 항목 점수 계산
│
├── common/
│   ├── BeamSearchConfig.java   # 공통 설정값
│   ├── BeamSearchContext.java  # 탐색 1회의 컨텍스트 (출발점, 목표 등)
│   ├── BeamSearchEngine.java   # 공통 탐색 루프 실행
│   └── BeamSearchStrategy.java # 전략 인터페이스
│
└── basic/
    ├── BeamSearchAlgorithm.java        # 외부 호출 진입점
    └── DefaultBeamSearchStrategy.java  # 기본 전략 구현
```

---

## 데이터 흐름

```
MySQL DB
  jeju_road_points (23만개 도로 포인트)
  obstacles        (신호등, 횡단보도)
  places           (편의점, 공원, 화장실)
        ↓
  GeoDataCache (앱 시작 시 전체 로드 → 메모리 캐시)
        ↓
  BeamSearchEngine.buildCandidates()
    └─ 반경 200m 내 후보만 꺼내 사용 (전체 스캔 없음)
        ↓
  ScoreCalculator.calculate()
        ↓
  최종 루트 → RouteService → API 응답
```

---

## 현재 한계 및 개선 방향

| 문제 | 원인 | 개선 방향 |
|------|------|----------|
| 루트 삐죽임 | OSM 막다른 길 포인트 포함 | 재수집 시 끝점 연결수 1개인 노드 제외 (완료) |
| 루트 3개 유사 | 빔 다양성 부족 | 초기 방향 분리 + 다양성 패널티 (완료) |
| 도보 비최적 | 차도 중심 포인트 혼재 | Tmap 보행 경로 데이터로 교체 검토 중 |
| 경치 정보 없음 | 관광지 데이터 미활용 | Visit Jeju API 수집 후 facility_bonus 강화 예정 |
